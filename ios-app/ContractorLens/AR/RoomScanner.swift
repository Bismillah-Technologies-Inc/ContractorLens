import Foundation
import RoomPlan
import ARKit

// This class is now a placeholder and is no longer used for session management.
// The session is now managed directly by the RoomCaptureViewRepresentable in ScanningView.swift.
class RoomScanner: NSObject, ObservableObject {
    // All session management logic has been removed to prevent conflicts.
}